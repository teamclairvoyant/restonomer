name = "checkpoint_basic_authentication_up"

request = {
  url = "https://postman-echo.com/basic-auth"
}

authentication = {
  authentication-type = "BasicAuthentication"
  credentials = {
    user-name = "postman"
    password = "password"
  }
}