name = "sample-checkpoint-invaid"

data= {
  data-request = {}

  data-response = {
    body-format = "JSON"

    persistence = {
      type = "file-system"
      file-format = "json"
      file-path = "/tmp"
    }
  }
}
