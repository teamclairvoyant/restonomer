name = "checkpoint_file_system_persistence"

data = {
  data-request = {
    url = "http://localhost:8080/file-system-persistence"
  }

  data-response = {
    body = {
      type = "JSON"
    }

    persistence = {
      type = "FileSystem"
      file-format = "parquet"
      file-path = "/tmp/persistence"
    }
  }
}
