name = "sample-checkpoint-valid"

data = {
  data-request = {
    url = "http://test-domain.com"
  }

  data-response = {
    body = {
      type = "JSON"
    }

    persistence = {
      type = "FileSystem"
      file-format = "JSON"
      file-path = "/tmp"
    }
  }
}
