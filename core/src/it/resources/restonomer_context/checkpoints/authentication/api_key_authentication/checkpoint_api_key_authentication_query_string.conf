name = "checkpoint_api_key_authentication_query_string"

request = {
  url = "http://localhost:8080/api-key-auth-query-string"

  authentication = {
    type = "api-key-authentication"
    api-key-name = "test_api_key_name"
    api-key-value = "test_api_key_value"
    placeholder = "QueryString"
  }
}

response = {
  body = {
    format = "JSON"
  }
}
