name = "checkpoint_retry_status_204"

request = {
  url = "http://localhost:8080/retry-status-204"
}

response = {
  body = {
    format = "JSON"
  }

  persistence = {
    type = "file-system"
    file-format = "json"
    file-path = "/tmp/retry"
  }
}