name = "sample-checkpoint-valid"

data = {
  data-request = {
    name = "request_no_authentication"
    url = "http://test-domain.com"
  }

  data-response = {
    body = {
      type = "json"
    }

    persistence = {
      type = "file-system"
      file-format = "json"
      file-path = "/tmp"
    }
  }
}
