name = "checkpoint_retry_status_200_success"

request = {
  url = "http://localhost:8080/retry-status-200-success"
}

response = {
  body = {
    format = "JSON"
  }

  persistence = {
    type = "file-system"
    file-format = "json"
    file-path = "/tmp/retry"
  }
}