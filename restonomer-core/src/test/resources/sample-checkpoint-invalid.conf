name = "sample-checkpoint-invaid"

data= {
  data-request = {}

  data-response = {
    body = {
      type = "json"
    }

    persistence = {
      type = "file-system"
      file-format = "json"
      file-path = "/tmp"
    }
  }
}
