name = "checkpoint_replace_StringValue_transformation"

data = {
  data-request = {
    url = "http://localhost:8080/replace-stringValue-transformation"
  }

  data-response = {
    body-format = "JSON"

    transformations = [
      {
        type = "replace-StringValue"
        column-name = "col_B"
        column-value = "val_B"
        column-replaceValue = "value_B"

      }
    ]

    persistence = {
      type = "file-system"
      file-format = "json"
      file-path = "/tmp/transformation"
    }
  }
}
