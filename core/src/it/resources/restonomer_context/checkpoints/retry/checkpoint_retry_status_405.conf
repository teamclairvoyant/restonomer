name = "checkpoint_retry_status_405"

request = {
  url = "http://localhost:8080/retry-status-405"
}

response = {
  body = {
    format = "JSON"
  }

  persistence = {
    type = "file-system"
    file-format = "json"
    file-path = "/tmp/retry"
  }
}