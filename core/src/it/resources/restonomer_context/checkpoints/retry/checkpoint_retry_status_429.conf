name = "checkpoint_retry_status_429"

request = {
  url = "http://localhost:8080/retry-status-429"
}

response = {
  retry = {
    max-retries = 1
    status-codes-to-retry = [429]
  }

  body = {
    format = "JSON"
  }

  persistence = {
    type = "file-system"
    file-format = "json"
    file-path = "/tmp/retry"
  }
}
