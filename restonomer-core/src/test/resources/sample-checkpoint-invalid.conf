name = "sample-checkpoint-invaid"

data = {
  data-request = {}

  data-response = {
    body = {
      type = "JSON"
    }

    persistence = {
      type = "file-system"
      file-format = {
        type = "JSONFileFormat"
      }
      file-path = "/tmp"
    }
  }
}
