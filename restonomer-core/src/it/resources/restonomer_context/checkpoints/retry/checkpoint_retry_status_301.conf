name = "checkpoint_retry_status_301"

data = {
  data-request = {
    url = "http://localhost:8080/retry-status-301"
  }

  data-response = {
    body = {
      type = "json"
    }

    persistence = {
      type = "file-system"
      file-format = "json"
      file-path = "/tmp/retry"
    }
  }
}
