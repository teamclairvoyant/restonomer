name = "sample-checkpoint-invaid"

data = {
  data-request = {}

  data-response = {
    body = {
      type = "Text"
      text-format = {
        type = "JSONTextFormat"
      }
    }

    persistence = {
      type = "file-system"
      file-format = {
        type = "JSONFileFormat"
      }
      file-path = "/tmp"
    }
  }
}
