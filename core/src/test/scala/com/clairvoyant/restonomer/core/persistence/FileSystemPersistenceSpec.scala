package com.clairvoyant.restonomer.core.persistence

import com.clairvoyant.restonomer.core.CoreSpec

class FileSystemPersistenceSpec extends CoreSpec {

  /*import sparkSession.implicits._

  "persist() - with proper format and path" should "save the dataframe" in {
    val fileSystemPersistence = FileSystem(
      fileFormat = "json",
      filePath = ""
    )

    val responseDF = Seq(("val_A", "val_B", "val_C", "val_D"))
      .toDF("col_A", "col_B", "col_C", "col_D")

    fileSystemPersistence.persist(responseDF)
  }*/

}
