name = "checkpoint_retry_status_405"

data = {
  data-request = {
    url = "http://localhost:8080/retry-status-405"
  }

  data-response = {
    body-format = "JSON"

    persistence = {
      type = "file-system"
      file-format = "json"
      file-path = "/tmp/retry"
    }
  }
}
