name = "sample-checkpoint-valid"

data = {
  data-request = {
    url = "http://test-domain.com"
  }

  data-response = {
    body = {
      type = "Text"
      text-format = {
        type = "JSONTextFormat"
      }
    }

    persistence = {
      type = "LocalFileSystem"
      file-format = {
        type = "JSONFileFormat"
      }
      file-path = "/tmp"
    }
  }
}
